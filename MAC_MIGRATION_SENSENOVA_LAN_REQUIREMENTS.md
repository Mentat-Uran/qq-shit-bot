# Mac 迁移、SenseNova 视觉路由与局域网运行面板需求计划

## 1. 计划定位

- 本计划只描述需要完成的需求、边界和验收标准，不规定具体实现方式。
- 目标部署主机为安装 Docker Desktop 的 Mac 笔记本。
- Mac 作为 QQ Bot 的主要运行主机，并向同一局域网内的 Windows 电脑提供 Web 运行面板访问能力。
- Mac 不使用本地 Qwen2.5-VL 或 Ollama 进行图片识别。
- 图片识别统一使用 SenseNova 6.7 Flash-Lite，文本理解和最终回复继续使用 DeepSeek 路由。
- 当前仓库原样直接运行不属于本计划的可接受结果。

## 2. 总体目标

- 项目能够在 Mac Docker Desktop 环境中完成可重复部署、启动、停止、更新和健康检查。
- QQ 文字消息、图片消息、私聊消息、群聊 @ 消息和现有上下文策略在 Mac 上保持一致。
- 图片消息能够经过 SenseNova 6.7 Flash-Lite 识别后，再交给文本模型生成最终回复。
- Mac 不再承担本地视觉模型权重、NVIDIA GPU、CUDA 或 Ollama 的运行要求。
- Windows 电脑能够在同一局域网内访问 Mac 上的运行面板。
- OpenClaw 原生 Control UI 与项目自有 QQ Bot Operations Console 的职责、端口和安全边界保持清晰。
- Windows 现有的本地 Qwen 部署能力不因 Mac 迁移而被无意破坏。

## 3. 不可接受的结果

- 仍要求 Mac 具备 NVIDIA GPU、CUDA 或 `gpus: all` 才能启动。
- Mac 启动过程中仍自动下载或启动 Ollama、Qwen2.5-VL 或本地视觉模型。
- 只修改模型名称，但图片附件不能实际到达 SenseNova。
- 只让本机访问面板，Windows 无法通过局域网访问。
- 通过无认证的局域网端口公开 QQ 日志、会话、密钥状态或诊断信息。
- 为了 Mac 迁移删除或破坏现有 Windows 启动链路。
- 将 `.env`、Docker volume、模型缓存、日志、SQLite 状态或其他运行时私有数据同步到云端仓库。
- 将健康检查、容器运行或本机连接状态误称为 QQ 外部消息已经成功收发。

## 4. Mac 平台部署需求

### 4.1 基础运行

- 支持在 Mac Docker Desktop 中运行完整的 QQ Bot 网关链路。
- 支持 Apple Silicon Mac。
- 对 Intel Mac 的行为、镜像架构和兼容性边界需要有明确说明。
- Mac 部署不得依赖 Windows BAT、Windows 专用 PowerShell 启动入口或 Windows 专用主机采集能力。
- Mac 部署必须提供明确的启动、停止、状态、日志和环境检查入口。
- Mac 部署必须保留现有容器安全限制、工具禁用策略、QQ 诊断过滤和上下文恢复能力。
- Mac 部署必须继续使用外部 `.env` 管理凭据，凭据不得进入镜像、源码、日志或 Web 页面。

### 4.2 服务组成

- Mac 默认运行 OpenClaw Gateway。
- Mac 默认运行 context-recovery 守护服务。
- Mac 默认不运行 `qwen-vision`。
- Mac 默认不运行 Ollama。
- Mac 默认不包含 Qwen 模型缓存卷。
- Mac 默认不启用视频识别、图像融合或已归档的重量级视觉服务。
- 可选的管理 CLI 不得被误认为常驻服务。

### 4.3 资源要求

- 文档需要说明 Mac 运行时的建议内存、磁盘空间和网络要求。
- 文档需要区分首次下载镜像、运行时空间、日志空间和未来升级空间。
- 文档需要明确云端视觉模型不会在 Mac 上产生本地模型权重占用。

## 5. SenseNova 视觉路由需求

### 5.1 模型与职责

- 图片识别模型必须使用 SenseNova 6.7 Flash-Lite。
- 图片识别模型必须支持图片输入和文本输入。
- 图片识别模型必须能够处理普通图片、截图、表情包、OCR 场景和当前项目允许的图片消息。
- SenseNova 视觉识别只负责提供视觉理解结果，不直接替代 QQ 最终回复模型。
- DeepSeek 文本模型负责结合图片识别结果、当前消息和允许的上下文生成最终回复。
- 必须明确文本主模型是 SenseNova 平台上的 DeepSeek V4 Flash，还是官方 DeepSeek Chat，不能让路由含义模糊。
- 主文本模型和备用文本模型的顺序、失败条件和用户可见行为必须有文档记录。

### 5.2 图片数据处理

- 当前 QQ 图片附件必须以实际图片数据进入视觉模型请求。
- 图片文件名、占位符、历史图片描述或不可靠的 QQ 图片 URL 不得被当作图片本体。
- 必须支持 SenseNova 所需的图片输入格式。
- 私有、临时或不可公开访问的 QQ 图片地址不得直接作为外部模型的可访问 URL 依赖。
- 图片无法取得、下载失败、格式不支持或视觉模型超时时，系统必须产生可控的短回复或安全降级行为。
- 图片识别失败时不得向 QQ 用户泄露 API Key、HTTP 状态、供应商、配额、限流、堆栈或内部模型诊断。
- 图片识别结果必须只保留必要的文字内容，不得把模型内部 reasoning 直接传给用户或无必要地写入长期状态。
- 图片识别结果需要能够表达不确定性，不能把低置信度识别结果伪装成确定事实。

### 5.3 识别结果到文本模型

- 视觉识别结果必须能够进入 DeepSeek 的文本上下文。
- DeepSeek 不得被强制接收其配置不支持的原始图片输入。
- 识别结果必须与当前 QQ 消息和允许的上下文保持关联。
- 图片消息的现有 @ 提及门控、当前消息限制和历史媒体限制必须继续生效。
- 视觉模型和文本模型的超时、重试、取消和 fallback 行为必须互相隔离。
- 视觉模型失败不得导致整个 QQ Gateway 会话永久卡死。
- 视觉模型和文本模型的诊断必须继续受到现有 QQ 输出过滤策略约束。

## 6. QQ 消息行为需求

- 纯文字消息不得无故调用视觉模型。
- 当前消息直接附图并 @ 机器人时，必须允许视觉识别。
- 先发送图片、随后发送 @ 消息的既有移动端流程必须继续支持，前提是实际图片附件仍然可取得。
- 历史消息中的旧图片不得因为被动提及而自动污染新的识别请求。
- 普通表情包、梗图和非视觉问题必须继续遵守现有简短回复规则。
- 视频不得因为 SenseNova 图片能力接入而被误认为已支持视频识别。
- QQ 群、私聊、引用、回复和主动巡检行为必须继续遵守现有项目规则。

## 7. 局域网 Web 访问需求

### 7.1 OpenClaw Control UI

- OpenClaw Control UI 必须具备可配置的监听地址和端口。
- 默认安全行为仍应是仅本机访问。
- Mac 局域网部署必须能够显式开启局域网访问。
- Windows 电脑必须能够通过 Mac 的局域网地址访问 Control UI（如明确需要该界面）。
- Control UI 必须继续要求 Gateway Token 认证。
- Token 不得显示在 Operations Console、诊断摘要、日志或 URL 查询参数中。
- 不得要求路由器进行公网端口转发。

### 7.2 QQ Bot Operations Console

- Operations Console 必须支持 Mac 主机运行。
- Operations Console 必须具备可配置的监听地址和端口。
- 默认本机模式必须继续保留。
- 局域网模式必须允许同一局域网内的 Windows 电脑访问。
- 控制台必须明确显示自身监听地址、端口和访问模式。
- 控制台必须区分本机探测地址、Mac 服务地址和浏览器访问地址。
- 控制台中的 OpenClaw Control UI 链接必须指向 Windows 浏览器可访问的 Mac 地址，而不是错误的 Windows `127.0.0.1`。
- 控制台必须支持 Gateway、context-recovery、Docker 服务、QQ 连接证据、会话状态、日志摘要和资源状态展示。
- SenseNova 云端视觉模式下，NVIDIA GPU、VRAM 和 Ollama 状态必须显示为不适用、未启用或未知，不能伪造为正常 GPU 运行。
- 控制台不能因为缺少 NVIDIA GPU 而把整个 Mac 部署误判为故障。

### 7.3 局域网安全

- 局域网暴露必须是显式配置，不得改变默认的本机安全边界。
- 监听范围应能够限制到 Mac 的局域网地址或受控网卡。
- 绑定到所有网卡时必须有清晰的安全警告和访问保护。
- Operations Console 不能以“只读”为理由免除访问控制；它展示的日志、会话和 QQ 活动仍属于敏感运行信息。
- 局域网访问必须至少具备明确的认证、访问令牌或等效保护机制。
- Web 服务不得暴露任意 Shell、PowerShell、Docker、文件系统或 Docker Socket 执行能力。
- 浏览器端不得直接访问 Docker Socket。
- 高风险操作不得因局域网暴露而自动开放。

## 8. macOS 运行状态采集需求

- 控制台必须支持 macOS CPU、内存、磁盘和 Docker 状态采集。
- 无法采集的指标必须明确显示未知，并说明证据边界。
- 不得把 Windows 专用采集结果套用到 Mac。
- 不得把 Docker Desktop 健康状态等同于 QQ 外部收发成功。
- 资源采集失败不得影响 QQ Gateway 的正常回复。
- 采集结果必须继续经过脱敏处理。

## 9. Windows 兼容需求

- 现有 Windows + Qwen 部署路径必须继续可用，除非另有明确废弃决定。
- Windows 的 Qwen 本地视觉模式和 Mac 的 SenseNova 云视觉模式必须能够被明确区分。
- Windows 不得因为新增 Mac 配置而意外启动 Mac 的 SenseNova 图片路由。
- Windows 现有 BAT、PowerShell、Docker Compose 和视觉能力策略不得被无意删除。
- 平台差异必须在文档和配置命名中清晰表达。

## 10. 仓库同步与协作需求

### 10.1 Windows 端作为开发源

- Mac 迁移相关需求必须继续在当前仓库中开发和审查。
- 当前工作区已有的其他修改不得被迁移计划覆盖、丢弃或自动混入。
- 迁移内容必须能够单独识别、单独审查和单独同步。
- 迁移内容必须通过本地配置、静态检查、单元测试和文档审查。
- 云端同步内容必须来自明确的迁移提交，而不是未经审查的整个工作区快照。

### 10.2 云端仓库内容

- 云端只保存可复现的源码、配置模板、脚本、测试和文档。
- `.env`、API Key、Secret、Cookie、QQ 身份信息不得进入云端。
- runtime 状态、日志、SQLite 数据库、Docker volume 和模型缓存不得进入云端。
- 云端提交必须能够让 Mac 从干净工作区获取同一版本的部署材料。
- Mac 部署分支、提交或版本标识必须可追踪。

### 10.3 Mac 端同步

- Mac 必须从云端获取迁移后的源码和配置模板。
- Mac 必须独立创建本机 `.env` 和运行时目录。
- Mac 的本机 IP、端口、Token 和本地状态不得反向写入公共配置。
- Mac 上发现的代码问题必须能够以源码变更形式同步回云端。
- Mac 运行时生成的数据不得被误提交为源码修改。

## 11. 验收需求

### 11.1 静态与配置验收

- Mac Compose 配置能够完成配置解析。
- Mac 配置不包含强制 NVIDIA、CUDA、Ollama 或 Qwen 依赖。
- Mac 图片路由指向 SenseNova 6.7 Flash-Lite。
- DeepSeek 文本路由和 fallback 顺序符合项目决策。
- LAN 监听默认关闭或保持本机绑定。
- LAN 模式的监听地址、端口和认证配置可被审查。
- Windows 配置仍保留原有 Qwen 路径。

### 11.2 视觉路由验收

- SenseNova 文本请求成功。
- SenseNova 单张图片请求成功。
- QQ 实际图片附件能够进入 SenseNova 请求。
- OCR、普通图片描述和图片问答均有验证结果。
- SenseNova 失败时有安全降级。
- DeepSeek 能够收到视觉识别文本并生成最终回复。
- 视觉模型内部诊断不会进入 QQ。

### 11.3 局域网验收

- Mac 本机可以访问 Operations Console。
- Windows 同局域网设备可以访问 Operations Console。
- Windows 可以访问明确开放的 OpenClaw Control UI，且 Token 认证有效。
- 默认本机模式仍不会向局域网开放。
- 非授权访问被拒绝或无法获得敏感运行数据。
- Mac 更换局域网地址时，访问地址和文档行为明确。

### 11.4 真实边界验收

- 容器健康不能被记录为 QQ 外部消息已送达。
- 必须有至少一次真实 QQ 文字消息验证。
- 必须有至少一次真实 QQ 图片消息验证。
- 必须有一次从 Windows 浏览器访问 Mac 面板的验证。
- 所有未实际观察到的外部行为必须标记为未验证。

## 12. 交付物需求

- Mac 专用部署配置。
- SenseNova 视觉路由配置。
- Mac 启动、停止、状态和环境检查入口。
- 可配置的 Gateway 局域网访问能力。
- 可配置的 Operations Console 局域网访问能力。
- Operations Console 的 macOS 采集支持。
- LAN 访问安全说明。
- Windows 与 Mac 双平台部署说明。
- 云端同步边界说明。
- Mac 迁移测试和真实验证记录模板。
- Windows 现有部署兼容性说明。
- 本计划文件本身必须同步到云端，作为后续开发窗口的需求基线。

## 13. 完成判定

只有同时满足以下条件，Mac 迁移才算完成：

- Mac 能在不安装本地 Qwen/Ollama 的情况下运行 QQ Bot。
- 图片能由 SenseNova 6.7 Flash-Lite 识别。
- DeepSeek 能基于识别结果生成最终回复。
- Windows 能通过局域网访问 Mac 的运行面板。
- Control UI 和 Operations Console 的认证与暴露范围清晰。
- Windows 原有部署路径未被破坏。
- 迁移源码、测试和文档已同步到云端。
- `.env`、运行时状态和模型缓存未进入云端。
- 真实 QQ 文字、图片和跨设备面板访问均有独立验证证据。

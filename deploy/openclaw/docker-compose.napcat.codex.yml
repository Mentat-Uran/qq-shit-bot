# NapCat and the adapter use the same host-local reverse WebSocket endpoint in
# the Linux Codex overlay. start-onebot.sh applies NAPCAT_WEBUI_BIND_ADDRESS
# to NapCat's persisted WebUI config; the default remains loopback-only.
services:
  napcat:
    network_mode: host
    ports: !reset []
